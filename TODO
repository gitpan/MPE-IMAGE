1) The following calls need to be handled:
     DbLock, DbPut, DbUpdate, DbDelete, DbUnlock

2) It would be nice if README were automatically built just prior to creating
   the distribution.

3) All calls which accept item and set numbers should accept either negative or
   positive values.
